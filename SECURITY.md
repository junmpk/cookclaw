# Security policy

Please do not open a public issue containing secrets, personal data, production
addresses or device identifiers. Report a suspected vulnerability privately through
the repository owner's GitHub security contact.

If a credential is committed accidentally, revoke it first, then remove it from the
entire Git history. Deleting it only from the latest commit is not sufficient.

